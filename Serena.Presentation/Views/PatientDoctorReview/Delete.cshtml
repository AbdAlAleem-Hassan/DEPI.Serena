@model Serena.BLL.Models.PatientDoctorReviews.PatientDoctorReviewGetDTO

@{
    ViewData["Title"] = "Delete Patient Review";
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card shadow mb-4 border-left-danger">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-danger">Delete Patient Review</h6>
            </div>
            <div class="card-body">
                <h4 class="small font-weight-bold">Are you sure you want to delete this review?</h4>
                <p class="mb-4">This action cannot be undone.</p>
                
                <dl class="row">
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.PatientId)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.PatientId)
                    </dd>
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.DoctorId)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.DoctorId)
                    </dd>
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Rating)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Rating)
                    </dd>
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Comment)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Comment)
                    </dd>
                </dl>
                
                <form asp-action="Delete">
                    <input type="hidden" name="patientId" value="@Model.PatientId" />
                    <input type="hidden" name="doctorId" value="@Model.DoctorId" />
                    <div class="mt-4 text-center">
                        <input type="submit" value="Delete" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
